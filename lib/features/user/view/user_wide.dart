import 'package:flutter/cupertino.dart';

class UserWide extends StatelessWidget {
  const UserWide({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('UserWide'));
  }
}