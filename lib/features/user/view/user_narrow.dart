import 'package:flutter/cupertino.dart';

class UserNarrow extends StatelessWidget {
  const UserNarrow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('UserNarrow'));
  }
}