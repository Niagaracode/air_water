import 'package:flutter/cupertino.dart';

class CustomerDashboardMiddle extends StatelessWidget {
  const CustomerDashboardMiddle({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(16),
      child: Text('CustomerDashboardMiddle'),
    );
  }
}