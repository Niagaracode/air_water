import 'package:flutter/cupertino.dart';

class CustomerDashboardWide extends StatelessWidget {
  const CustomerDashboardWide({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(16),
      child: Text('CustomerDashboardWide'),
    );
  }
}