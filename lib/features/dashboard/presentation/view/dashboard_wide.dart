import 'package:flutter/cupertino.dart';

class DashboardWide extends StatelessWidget {
  const DashboardWide({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('DashboardWide'));
  }
}