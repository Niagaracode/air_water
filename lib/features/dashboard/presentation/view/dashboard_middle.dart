import 'package:flutter/cupertino.dart';

class DashboardMiddle extends StatelessWidget {
  const DashboardMiddle({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Dashboard Middle'));
  }
}