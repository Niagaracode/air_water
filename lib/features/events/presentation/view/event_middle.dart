import 'package:flutter/material.dart';
import 'event_wide.dart';

class EventMiddle extends StatelessWidget {
  const EventMiddle({super.key});

  @override
  Widget build(BuildContext context) {
    return const EventWide();
  }
}
