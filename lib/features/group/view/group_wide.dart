import 'package:flutter/cupertino.dart';

class GroupWide extends StatelessWidget {
  const GroupWide({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('GroupWide'));
  }
}