import 'package:flutter/material.dart';

class AssetScheduleMiddle extends StatelessWidget {
  const AssetScheduleMiddle({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Asset Schedule')),
      body: const Center(child: Text('Tablet view coming soon')),
    );
  }
}
