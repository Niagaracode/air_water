import 'package:flutter/material.dart';

class AssetScheduleNarrow extends StatelessWidget {
  const AssetScheduleNarrow({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Asset Schedule')),
      body: const Center(child: Text('Mobile view coming soon')),
    );
  }
}
