import 'package:flutter/cupertino.dart';

class ProductNarrow extends StatelessWidget {
  const ProductNarrow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('ProductNarrow'));
  }
}