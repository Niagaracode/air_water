import 'package:flutter/cupertino.dart';

class ProductWide extends StatelessWidget {
  const ProductWide({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('ProductWide'));
  }
}