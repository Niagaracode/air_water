import 'package:flutter/cupertino.dart';

class ProductMiddle extends StatelessWidget {
  const ProductMiddle({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('ProductMiddle'));
  }
}