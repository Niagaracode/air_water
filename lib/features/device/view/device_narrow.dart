import 'package:flutter/cupertino.dart';

class DeviceNarrow extends StatelessWidget {
  const DeviceNarrow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('DeviceNarrow'));
  }
}