import 'package:flutter/cupertino.dart';

class DeviceWide extends StatelessWidget {
  const DeviceWide({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('DeviceWide'));
  }
}