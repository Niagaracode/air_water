import 'package:flutter/cupertino.dart';

class DeviceMiddle extends StatelessWidget {
  const DeviceMiddle({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('DeviceMiddle'));
  }
}