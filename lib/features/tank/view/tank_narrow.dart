import 'package:flutter/cupertino.dart';

class TankNarrow extends StatelessWidget {
  const TankNarrow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('TankNarrow'));
  }
}