
import 'package:flutter/cupertino.dart';

class TankWide extends StatelessWidget {
  const TankWide({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('TankWide'));
  }
}