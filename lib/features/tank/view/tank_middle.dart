import 'package:flutter/cupertino.dart';

class TankMiddle extends StatelessWidget {
  const TankMiddle({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('TankMiddle'));
  }
}