import 'package:flutter/material.dart';
import 'rule_wide.dart';

class RuleNarrow extends StatelessWidget {
  const RuleNarrow({super.key});

  @override
  Widget build(BuildContext context) {
    return const RuleWide();
  }
}